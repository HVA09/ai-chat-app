import { useTranslation } from "react-i18next";
const MAX_MESSAGE_LENGTH = 4000;
export default function ChatComposer({ value, setValue, onSend, onStop, loading }) { const { t } = useTranslation(); const nearLimit = value.length > MAX_MESSAGE_LENGTH - 200; return <form onSubmit={(e) => { e.preventDefault(); if (!value.trim() || loading) return; onSend(); }} className="border-t border-slate-200 bg-white p-4"><div className="flex items-end gap-3"><div className="flex-1"><textarea value={value} onChange={(e) => setValue(e.target.value)} placeholder={t("placeholder")} rows={2} maxLength={MAX_MESSAGE_LENGTH} disabled={loading} className="min-h-[56px] w-full resize-none rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 outline-none disabled:opacity-60" />{nearLimit && <p className="mt-1 text-end text-xs text-slate-400">{value.length}/{MAX_MESSAGE_LENGTH}</p>}</div>{loading ? <button type="button" onClick={onStop} className="rounded-2xl border border-red-200 bg-red-50 px-5 py-3 text-red-700">{t("stop")}</button> : <button type="submit" disabled={!value.trim()} className="rounded-2xl bg-slate-900 px-5 py-3 text-white disabled:opacity-50">{t("send")}</button>}</div></form>; }
